{
  "name": "qeli-frontoffice-cypress",
  "version": "0.0.1",
  "description": "Cypress avec Cucumber pour tester l'application web",
  "scripts": {
    "cy:verify": "cypress verify",
    "cy:version": "cypress version",
    "cy:open:ci": "CYPRESS_ENV=ci npm run cy:open",
    "cy:open:prod": "CYPRESS_ENV=prod npm run cy:open",
    "cy:open": "cypress open",
    "cy:run:ci": "CYPRESS_ENV=ci npm run test",
    "cy:run:prod": "CYPRESS_ENV=prod npm run test",
    "test": "cypress run",
    "test:pull-features": "git submodule update --remote features-metier && cp -rf features-metier/* cypress/integration && node ./scripts/remove-old-features.js"
  },
  "private": true,
  "dependencies": {
    "cypress": "~3.8.2",
    "cypress-cucumber-preprocessor": "~2.0.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  }
}
